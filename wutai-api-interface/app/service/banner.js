'use strict';

const Service = require('egg').Service;

class BannerService extends Service {
  async index() {
      // 章节
    //   this.app.model.banner.findAll({})
  }
}

module.exports = BannerService;
